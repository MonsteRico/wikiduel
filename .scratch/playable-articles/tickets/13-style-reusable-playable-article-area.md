> *This was generated from the Playable Article Lab follow-up request.*

# Style the Reusable Playable Article Area

Status: completed
Scope: MVP required
Category: enhancement

## Parent

- [`playable-articles/10`](./10-render-typed-article-documents.md)
- [Playable Articles spec](../spec.md)

## What to build

Create the reusable visual treatment for the Playable Article area that appears
in the production Duel screen and the development-only Playable Article Lab.
The surface should make the Lab representative of gameplay while keeping the
typed Article Document renderer independent of Duel state, Lab diagnostics, and
WebSocket transport.

## Acceptance criteria

- [x] The Lab and Duel article areas share one responsive, reusable visual
  treatment rather than maintaining separate article-specific styling.
- [x] Headings, paragraphs, lists, Navigation Nodes, figures, captions, and
  article/image attribution remain readable and visually coherent in the
  shared area.
- [x] The article surface preserves keyboard-visible Navigation focus and
  usable layouts at narrow and wide viewport sizes.
- [x] The styling does not change Article Document semantics, Navigation
  behavior, attribution requirements, or the authoritative Duel contract.
- [x] The boundary between shared article presentation and Duel-specific
  controls is documented well enough for the future Duel screen to reuse it.

## Implementation note

The client now wraps the semantic `ArticleDocumentRenderer` in a reusable
`PlayableArticleArea` that owns the selected Duel-like frame, light article
canvas, responsive figure layout, typography, and attribution styling. The Lab
uses that shared boundary directly, while diagnostics, WebSocket transport, and
future Duel controls remain outside the article presentation boundary documented
in the client design system.

## Blocked by

- [`playable-articles/10`](./10-render-typed-article-documents.md)

## Triage note

Ticket 10 intentionally delivered a semantic renderer with minimal visual
assumptions so it could be reused by the Lab. Use the Lab and the intended Duel
layout to settle spacing, typography, figure treatment, attribution placement,
responsive behavior, and the presentation boundary before implementation.

## Comments

> *This was generated by AI during triage.*

2026-07-12: Triage confirmed this is still an enhancement and not already
implemented. The blocker is cleared because `playable-articles/10` is completed,
but the Lab currently renders the bare `ArticleDocumentRenderer` directly and no
shared article-area presentation component exists. A throwaway UI prototype now
lives on the existing development `/lab` route with `?variant=A`, `?variant=B`,
and `?variant=C` options for choosing the reusable article treatment. Keep this
ticket in `needs-triage` until a direction is chosen; then move it to
`ready-for-agent` with instructions to delete the prototype switcher and fold the
winning treatment into production-quality shared article presentation.

> *This was generated by AI during triage.*

2026-07-12: Direction chosen. Use prototype `?variant=B` as the implementation
target after its hybrid update: keep B's Duel-viewport frame, compact article
header, constrained scrolling area, and floated image layout; use A's serif
article typography, calmer heading rules, prose rhythm, list spacing, link
color, and attribution styling. Implementation should delete the throwaway
prototype switcher and losing variants, extract the chosen treatment into a
production-quality reusable article-area component or style boundary, and mount
that boundary from the Lab without coupling `ArticleDocumentRenderer` to Lab,
Duel, diagnostics, or WebSocket state.

## Agent brief

> *This was generated by AI during triage.*

Implement the reusable Playable Article visual area using the selected hybrid
from the `/lab?variant=B` prototype. Keep the semantic
`ArticleDocumentRenderer` independent and wrap it with shared presentation owned
by the playable-article client feature or another appropriate shared UI boundary.

The chosen treatment is:

- Overall layout: B's Duel-like viewport with dark game chrome, a compact
  current-article header, page ID affordance, and an internal scrollable light
  article canvas.
- Figure behavior: B's floated right image treatment on wider screens, collapsing
  to full-width figures on narrow screens.
- Typography: A's serif body voice, relaxed prose rhythm, serif article title and
  section headings, subdued Wikipedia-like link color, readable lists, and clear
  attribution/footer treatment.

Remove prototype-only code as part of the implementation: `?variant=` handling,
the floating switcher, and losing variants. Preserve Navigation Node focus
visibility and callback behavior, article/image attribution visibility, semantic
HTML from the renderer, and the production WebSocket/Duel contract. Document the
boundary between shared article presentation and Duel-specific controls so the
future Duel screen can reuse the same area.
